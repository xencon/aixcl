# Virtual environments (should be created fresh in container)
.venv/
venv/
env/
ENV/

# Python cache and build artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Git
.git/
.gitignore
.gitattributes

# Environment files (should be passed via env vars, not copied)
.env
.env.local
.env.*.local

# Data files (should be mounted as volumes, not copied)
data/
*.db
*.sqlite

# Test files (not needed in production image)
tests/
test_*.py
*_test.py
TESTING.md

# Documentation (not needed in production image)
docs/
*.md
README.md

# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml

# Lock files (uv.lock should be included, but we can exclude if rebuilding)
# uv.lock  # Keep this commented - we want to include it for reproducible builds
